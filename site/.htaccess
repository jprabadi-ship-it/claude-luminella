# Redirect plain HTTP to HTTPS. Scoped to this subdomain's directory only.
<IfModule mod_rewrite.c>
RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^(.*)$ https://claude-luminella.gigowat.com/$1 [R=301,L]
</IfModule>
